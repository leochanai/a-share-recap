{
  "name": "a-share-recap",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "sync": "rsync -av --delete ~/reports/*.md src/content/reports/",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "@astrojs/tailwind": "^5.1.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.15"
  }
}
